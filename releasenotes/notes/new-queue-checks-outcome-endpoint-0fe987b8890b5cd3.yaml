---
features:
  - |
    Added a new statistics endpoint that returns the queue_checks_outcome data
    for every partition and queues of the repository.
    The endpoint path is "/v1/repos/{repository_owner}/{repository_name}/stats/queue_checks_outcome".
